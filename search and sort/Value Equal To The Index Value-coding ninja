o(n) || o(1)

#include <bits/stdc++.h> 
vector<int> valueEqualToIndices(vector<int>& arr, int n) {
	vector<int>ans;
	for(int i=0;i<n;i++)
	{
		if(arr[i]==i)ans.push_back(i);
	}
	if(ans.size()==0)ans.push_back(-1);
	return ans;
}
